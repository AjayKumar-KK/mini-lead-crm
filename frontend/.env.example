# Optional: hit the mock API directly instead of going through the Vite dev proxy.
# Leave unset in development — vite.config.ts proxies /api -> http://localhost:4000 for you.
# VITE_API_BASE=http://localhost:4000
